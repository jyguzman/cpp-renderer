#include "obj.h"
#include "geometry.hpp"
#include <iostream>
#include <fstream>
#include <string>
#include <future>

//std::vector<std::string> split(std::string str) {
//	std::vector<std::string> result;
//	for (auto it = str.begin(); it != str.end(); ++it) {
//		
//	}
//}

//Mesh load_obj_file(const std::string path) {
//	Mesh obj;
//	std::ifstream file(path);
//	if (!file.is_open()) {
//		std::cerr << "Failed to open OBJ file: " << path;
//		return obj;
//	}
//
//	/*for (std::string line; std::getline(file, line); ) {
//		if (line.at(0) == 'v') {
//			auto v1 = line.at()
//		}
//	}*/
//
//	return obj;
//}

//Mesh load_obj(const std::string content) {
//	Mesh mesh();
//	return mesh();
//}